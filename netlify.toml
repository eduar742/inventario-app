[build]
  command   = "npx expo export --platform web"
  publish   = "dist"

[build.environment]
  NODE_VERSION = "18"

# Redireciona todas as rotas para index.html (SPA)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
